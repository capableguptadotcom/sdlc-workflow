{
  "name": "pantry-ledger",
  "version": "0.0.0",
  "private": true,
  "description": "A local inventory ledger for a community pantry.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "lint": "node --check src/server.js && node --check test/server.test.js",
    "test": "node --test",
    "build": "npm run lint",
    "security": "npm audit --omit=dev"
  }
}
