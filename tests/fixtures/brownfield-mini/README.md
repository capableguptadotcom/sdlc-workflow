# Parcel Desk

Parcel Desk is a small Python service for creating shipment orders.

Run its tests with:

```bash
python -m unittest discover -s tests
```

The domain logic lives in `src/orders.py`. The test suite documents the
currently supported order states.
