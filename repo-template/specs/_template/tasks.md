# Tasks: <name>

Each task must deliver a testable vertical increment. Keep dependency edges
explicit; avoid layer-only tickets such as “build database” or “build UI.”

- [ ] T1 — <observable increment>
  - Acceptance criteria:
  - Depends on:
  - Deterministic verification:
  - Human review point:
